<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="PimpMyTeam.TeamCrudPage" Title="{Binding TeamViewModel.Team.Name}">
    <ScrollView>
        <StackLayout x:Name="teamCrudStack" Margin="20" VerticalOptions="StartAndExpand">
            <Label Text="Name" />
            <Entry Text="{Binding TeamViewModel.Name}" />
            <Button Text="Save"  Command="{Binding SubmitCommand}" />
            <Button Text="Delete" Command="{Binding DeleteCommand}" />
            <Button Text="Cancel" Command="{Binding CancelCommand}" />
        </StackLayout>
    </ScrollView>
</ContentPage>